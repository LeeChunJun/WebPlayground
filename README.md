# 扣钉坊

## 项目初始化完成，可执行以下命令：
```bash
   cd code-workshop
   npm install
   npm run format
   npm run dev
```   

### 可选：使用以下命令在项目目录中初始化 Git：
```bash
   git init && git add -A && git commit -m "initial commit"
```   